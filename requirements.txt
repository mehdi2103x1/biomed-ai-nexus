# BioMed AI Nexus — Liver Disease Prediction Platform
# Python 3.10–3.13 supported.
# Install with:  pip install -r requirements.txt

# --- Core app & data ---
streamlit>=1.36
pandas>=2.0
numpy>=1.26
scipy>=1.11
joblib>=1.3

# --- Machine learning ---
scikit-learn>=1.4
xgboost>=2.0

# --- Visualisation ---
plotly>=5.20
matplotlib>=3.8

# --- Image / deep learning ---
opencv-python-headless>=4.9
pillow>=10.0
# Image Analysis page (CNN + Grad-CAM). tensorflow-cpu is the lighter CPU-only
# build (no CUDA libs) suited to cloud hosting. Pinned >=2.18 so a wheel exists
# for the cloud's Python (3.12/3.13). If the free tier still runs out of memory,
# comment this line out again — every other page keeps working.
tensorflow-cpu>=2.18

# --- Reporting ---
fpdf2>=2.7
