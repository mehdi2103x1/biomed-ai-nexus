# BioMed AI Nexus — Liver Disease Prediction Platform
# Python 3.10–3.13 supported.
# Install with:  pip install -r requirements.txt

# --- Core app & data ---
streamlit>=1.36
pandas>=2.0
numpy>=1.26
scipy>=1.11
joblib>=1.3

# --- Machine learning ---
scikit-learn>=1.4
xgboost>=2.0

# --- Visualisation ---
plotly>=5.20
matplotlib>=3.8

# --- Image / deep learning ---
opencv-python-headless>=4.9
pillow>=10.0
# NOTE: TensorFlow (for the Image Analysis CNN + Grad-CAM page) is intentionally
# NOT installed on the free Streamlit Community Cloud tier: its size exceeds the
# tier's memory/disk limits and breaks the build. The image module degrades
# gracefully when TensorFlow is absent. To enable it (locally or on a paid tier),
# install:  pip install tensorflow
# tensorflow-cpu>=2.16

# --- Reporting ---
fpdf2>=2.7
